<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Description>
      Обшие инструменты в сборке Common. Есть сервисы с часто используемыми методами работы с текстом, для упрощения работы с встроенными ресурсами, файловой системой, сетью, сервис для запуска процессов в .net core приложении, сервис для поиска исключений внутри других исключений (включая AggregateException).
      Есть generic Tree.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="12.0.1-beta1" />
  </ItemGroup>

</Project>
