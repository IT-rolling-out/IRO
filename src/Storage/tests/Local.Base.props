<!-- Common settings for all projects, that will create nuget package. -->
<Project>
  <PropertyGroup>
    <Version>2.0.1</Version>
  </PropertyGroup>

  <!-- Remove current string or better create empty Common.Base.props if don`t want to use common build settings. 
       It will override local settings.
       If you wan`t not to override - use Directory.Build.props or import your own file in csproj. -->
  <Import Project="..\..\Common.Base.props" />
</Project>